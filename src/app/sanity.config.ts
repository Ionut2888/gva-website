/** Sanity project credentials.
 *  After creating your project at sanity.io, paste your projectId below. */
export const SANITY_CONFIG = {
  projectId: 'es1eh557', // ← only thing you need to fill in
  dataset: 'production',
  apiVersion: '2024-01-01',
} as const;
